import 'package:get/get.dart';

class AppRoutes {
  AppRoutes._();
  static final routes = [
    GetPage(name: name, page: page),
    GetPage(name: name, page: page),
    GetPage(name: name, page: page),
    GetPage(name: name, page: page),
    GetPage(name: name, page: page),
    GetPage(name: name, page: page),
    GetPage(name: name, page: page),
    GetPage(name: name, page: page),
    GetPage(name: name, page: page),
  ];
}
